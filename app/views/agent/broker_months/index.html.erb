<div> <h2 class="page-header">月推广效果表  <small>显示您每月的推广注册效果 </small> </h2></div>
<p id="notice"><%= notice %></p>
<%= render partial: "agent/shared/date_filter_and_export_excel", locals: {thispath: agent_broker_months_path} %>
<table class="table">
  <thead>
    <tr>
      <th>月份</th>
      <th>点击数</th>
      <th>注册数</th>
      <th>新注册并存款</th>
      <th>注册存款转化率</th>
    </tr>
  </thead>

  <tbody>
    <% @dates.each do |date| broker_month  = @broker_months.select{|month| month.effective_on == date}.first || BrokerMonth.new( effective_on: date ) %>
      <tr>
        <td><%= broker_month.effective_on.to_s(:year_month) %></td>
        <td><%= broker_month.clink_visits %></td>
        <td><%= broker_month.member_count %></td>
        <td><%= broker_month.valuable_member_count %></td>
        <td><%= broker_month.display_valuable_rate %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= will_paginate @dates %>
