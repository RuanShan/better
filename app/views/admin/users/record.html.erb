<% content_for :page_title do %>
  <%= link_to "会员管理", admin_users_url %> /
   会员详情
<% end %>

<%= render 'admin/shared/user_tabs', current: @record_for %>

<div>
  <%if @record_for == "bonus"%>
    <%=render "my/wallets/bonus_records", platform: "admin"%>
  <% else %>
    <%=render "my/#{@record_for}s/records", platform: "admin"%>
  <% end %>
</div>
