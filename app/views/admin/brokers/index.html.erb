<div> <h2 class="page-header">代理管理 </h2>
</div>
<div>
  <div class="">
    <%=form_tag search_admin_brokers_path, method:"get", class:"form-inline", role:"form" do%>
      <div class="form-group">
        <label for="start_date">代理名</label>
        <%=text_field_tag "nickname", '', class:"form-control"%>
      </div>
      <div class="form-group">
        <%=submit_tag "查询",  class:"form-control"%>
      </div>
    <%end%>
  </div>
  <div style="padding-top:20px;">
    <span>显示方式</span>
    <% tree_active = @display == "tree" ? "active" : ""%>
    <% list_active = @display == "list" ? "active" : ""%>
    <%= link_to "树形", admin_brokers_path(display: 'tree'), class:"btn btn-default #{tree_active}" %>
    <%= link_to "列表", admin_brokers_path(display: 'list'), class:"btn btn-default #{list_active}" %>
  </div>
  <div style="padding-top:20px;">
    <%= render partial: "records", locals: {display: @display} %>
  </div>
</div>
