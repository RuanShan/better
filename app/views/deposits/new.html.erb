<%= render partial: "users/profile" %>

<div class="row">
  <%= render partial: "shared/user_sidebar" %>
  <div class="col-md-10 user-content" >
    <div class="my-sub-title ng-binding">充值</div>

    <%= render 'form', deposit: @deposit %>

    <%= link_to 'Back', deposits_path %>
  </div>
</div>
